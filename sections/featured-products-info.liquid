<section class="featured-products-info">
  <div class="container">
    <!-- Section Header -->
    <div class="section-header">
      {% if section.settings.badge_text != blank %}
        <span class="section-badge">{{ section.settings.badge_text }}</span>
      {% endif %}
      {% if section.settings.heading != blank %}
        <h2 class="section-heading serif">{{ section.settings.heading }}</h2>
      {% endif %}
      {% if section.settings.description != blank %}
        <p class="section-description">{{ section.settings.description }}</p>
      {% endif %}
    </div>

    <!-- Products Grid -->
    <div class="products-grid">
      {% for block in section.blocks %}
        {% case block.type %}
          {% when 'product_card' %}
            <div class="product-card" {{ block.shopify_attributes }}>
              <div class="product-card-inner">
                <!-- Product Image -->
                <div class="product-image-wrapper">
                  {% if block.settings.image != blank %}
                    <img
                      src="{{ block.settings.image | image_url: width: 600 }}"
                      alt="{{ block.settings.name }}"
                      class="product-image"
                      width="600"
                      height="750"
                      loading="lazy"
                    >
                  {% else %}
                    <div class="product-placeholder">
                      {{ 'product-1' | placeholder_svg_tag }}
                    </div>
                  {% endif %}

                  {% if block.settings.scent != blank %}
                    <div class="product-scent-badge">{{ block.settings.scent }}</div>
                  {% endif %}
                </div>

                <!-- Product Content -->
                <div class="product-card-content">
                  {% if block.settings.name != blank %}
                    <h3 class="product-name serif">{{ block.settings.name }}</h3>
                  {% endif %}

                  {% if block.settings.short_description != blank %}
                    <p class="product-short-desc">{{ block.settings.short_description }}</p>
                  {% endif %}

                  {% if block.settings.learn_more_link != blank %}
                    <a href="{{ block.settings.learn_more_link }}" class="btn-learn-more">
                      {{ block.settings.learn_more_text | default: 'Learn More' }}
                      <svg
                        xmlns="http://www.w3.org/2000/svg"
                        width="16"
                        height="16"
                        viewBox="0 0 24 24"
                        fill="none"
                        stroke="currentColor"
                        stroke-width="2"
                      >
                        <line x1="5" y1="12" x2="19" y2="12"></line>
                        <polyline points="12 5 19 12 12 19"></polyline>
                      </svg>
                    </a>
                  {% endif %}
                </div>
              </div>
            </div>
        {% endcase %}
      {% endfor %}
    </div>
  </div>
</section>

<style>
  .featured-products-info {
    padding: 8rem 0;
    background-color: var(--color-background);
  }

  .section-header {
    text-align: center;
    margin-bottom: 5rem;
    max-width: 48rem;
    margin-left: auto;
    margin-right: auto;
  }

  .section-badge {
    color: var(--color-accent);
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.3em;
    font-size: 0.75rem;
    margin-bottom: 1.5rem;
    display: block;
  }

  .section-heading {
    font-size: clamp(2.5rem, 5vw, 4rem);
    font-weight: 700;
    margin-bottom: 1.5rem;
    line-height: 1.15;
  }

  .section-description {
    font-size: 1.125rem;
    color: rgba(26, 46, 38, 0.6);
    line-height: 1.6;
    font-weight: 300;
  }

  .products-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 3rem;
    max-width: 90rem;
    margin: 0 auto;
  }

  @media (min-width: 768px) {
    .products-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (min-width: 1024px) {
    .products-grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  .product-card {
    background-color: white;
    border-radius: 2rem;
    overflow: hidden;
    box-shadow: 0 10px 40px -10px rgba(26, 46, 38, 0.1);
    transition: all 0.3s;
    border: 1px solid rgba(26, 46, 38, 0.05);
  }

  .product-card:hover {
    transform: translateY(-8px);
    box-shadow: 0 20px 60px -10px rgba(26, 46, 38, 0.2);
  }

  .product-card-inner {
    display: flex;
    flex-direction: column;
    height: 100%;
  }

  .product-image-wrapper {
    position: relative;
    aspect-ratio: 4/5;
    overflow: hidden;
    background-color: var(--color-background);
  }

  .product-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.5s;
  }

  .product-card:hover .product-image {
    transform: scale(1.05);
  }

  .product-placeholder {
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    background-color: var(--color-background);
  }

  .product-scent-badge {
    position: absolute;
    top: 1.5rem;
    right: 1.5rem;
    background-color: var(--color-accent);
    color: white;
    padding: 0.5rem 1rem;
    border-radius: 9999px;
    font-size: 0.75rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    box-shadow: 0 4px 16px rgba(164, 124, 72, 0.4);
  }

  .product-card-content {
    padding: 2rem;
    display: flex;
    flex-direction: column;
    flex-grow: 1;
  }

  .product-name {
    font-size: 1.5rem;
    font-weight: 700;
    margin-bottom: 0.75rem;
    color: var(--color-primary);
  }

  .product-short-desc {
    font-size: 0.875rem;
    color: rgba(26, 46, 38, 0.6);
    line-height: 1.5;
    margin-bottom: 1.5rem;
    flex-grow: 1;
    font-weight: 300;
  }

  .btn-learn-more {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    background-color: var(--color-primary);
    color: white;
    padding: 0.875rem 1.5rem;
    border-radius: 9999px;
    font-weight: 700;
    font-size: 0.875rem;
    text-decoration: none;
    transition: all 0.3s;
    align-self: flex-start;
  }

  .btn-learn-more:hover {
    background-color: var(--color-secondary);
    gap: 0.75rem;
    transform: translateX(4px);
  }

  .btn-learn-more svg {
    flex-shrink: 0;
  }
</style>

{% schema %}
{
  "name": "Featured Products (Info)",
  "settings": [
    {
      "type": "header",
      "content": "Section Header"
    },
    {
      "type": "text",
      "id": "badge_text",
      "label": "Badge Text",
      "default": "Our Product Families"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Concentrated Excellence"
    },
    {
      "type": "textarea",
      "id": "description",
      "label": "Description",
      "default": "Three distinct formulas, each engineered to deliver industrial-strength cleaning power."
    }
  ],
  "blocks": [
    {
      "type": "product_card",
      "name": "Product Card",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Product Image"
        },
        {
          "type": "text",
          "id": "scent",
          "label": "Scent Badge",
          "default": "Original Black"
        },
        {
          "type": "text",
          "id": "name",
          "label": "Product Name",
          "default": "King Pine Original"
        },
        {
          "type": "textarea",
          "id": "short_description",
          "label": "Short Description",
          "default": "The legendary industrial-strength cleaner since 1939."
        },
        {
          "type": "text",
          "id": "learn_more_text",
          "label": "Button Text",
          "default": "Learn More"
        },
        {
          "type": "url",
          "id": "learn_more_link",
          "label": "Button Link"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Featured Products (Info)",
      "blocks": [
        {
          "type": "product_card",
          "settings": {
            "scent": "Original Black",
            "name": "King Pine Original",
            "short_description": "The legendary industrial-strength cleaner since 1939."
          }
        },
        {
          "type": "product_card",
          "settings": {
            "scent": "Gold Pine",
            "name": "King Pine Gold",
            "short_description": "Where elegance meets industrial power."
          }
        },
        {
          "type": "product_card",
          "settings": {
            "scent": "Lavender",
            "name": "King Pine Lavender",
            "short_description": "Industrial cleaning with calming aromatherapy."
          }
        }
      ]
    }
  ]
}
{% endschema %}
